Drop your updated résumé here as `resume.pdf`.

The site links to it from the nav ("Résumé") and the hero ("Résumé" button)
via the path `assets/resume.pdf`. Until this file exists, that link will 404.

To use a different filename, update `contact.resume` in src/content.js.
